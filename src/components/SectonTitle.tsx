import styled from "styled-components";

export const SectionTitle = styled.h2`
    color: white;
    //font-size: 55px;
    margin-bottom: 50px;
    font-size: calc((100vw - 900px) / (1920 - 900) * (55 - 45) + 45px);
`